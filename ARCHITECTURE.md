# دليل المعمارية والصيانة — المحلّل الذكي للشخصية

هذا الملف يوثّق قواعد بناء التطبيق حتى تبقى الصيانة والتطوير سهلَين وبلا مشاكل.

## 1) التقنيات
- الواجهة: React 18 + Vite + Tailwind CSS (وضع داكن بالكلاس `darkMode:'class'`).
- سطح المكتب: Tauri 2 (نواة Rust في `src-tauri`).
- بلا مكتبات ثقيلة غير ضرورية؛ الرادار والتصدير مبنيان يدوياً بلا اعتماديات.

## 2) بنية المجلدات
```
src/
  main.jsx            نقطة الدخول
  App.jsx             التوجيه بين التبويبات (كائن TABS)
  index.css           المتغيرات والأنماط (فاتح/داكن)
  store/AppContext.jsx  الحالة العامة (theme, tab, records, subject, sharedImage)
  services/
    ai.js             طبقة استدعاء Claude الوحيدة (لا تُستدعى الشبكة من مكان آخر)
    analyzers.js      تعريف كل تحليل + runAll للفحص المتوازي
    exportReport.js   توليد PDF/HTML
  components/         مكوّنات مشتركة (UI, ResultView, RadarChart, SharedImage, Sidebar, Topbar)
  tabs/              تبويب لكل قسم (ملف واحد لكل تبويب)
```

## 3) قواعد ذهبية (التزم بها عند أي تطوير)
1. كل استدعاء لنماذج الذكاء يمرّ حصراً عبر `services/ai.js`. لا تضع `fetch` لـ Anthropic في أي تبويب.
2. المفتاح لا يُكتب في الكود إطلاقاً — يُقرأ من `.env` (`VITE_ANTHROPIC_API_KEY`). لا تُسجّل المفاتيح في Git (`.gitignore`).
3. الحالة المشتركة تُدار في `AppContext` فقط. لا تُكرّر localStorage في المكوّنات.
4. الصورة الموحّدة `sharedImage` في الذاكرة فقط (تُحذف عند التحديث) — لا تُخزَّن في localStorage (حجمها كبير).
5. الألوان والمسافات عبر أدوات Tailwind ومتغيّرات `index.css`، لا ألوان صريحة متناثرة. راعِ الوضعين الفاتح والداكن دائماً.
6. النصوص عربية RTL؛ استخدم `dir="rtl"` والمحاذاة المناسبة.
7. مخرجات النماذج تُنسّق بعناوين `## ` ونقاط `- ` و`**تشديد**` ليعرضها `ResultView` بشكل موحّد.
8. لإضافة سمات OCEAN لأي تحليل، اطلب من النموذج سطر: `[[OCEAN O=.. C=.. E=.. A=.. N=..]]` ويلتقطه `RadarChart.parseOCEAN` تلقائياً.

## 4) كيف تضيف تبويباً جديداً
1. أنشئ `src/tabs/MyTab.jsx` يصدّر مكوّناً افتراضياً.
2. أضِفه في `App.jsx` داخل كائن `TABS` بمفتاح فريد.
3. أضِف عنصره في `components/Sidebar.jsx` ضمن المجموعة المناسبة.

## 5) الأمان والأداء
- تحقّق من حجم الصور قبل الإرسال، واضبط `max_tokens` حسب الحاجة.
- عند النشر: انتقل من المفتاح المحلي إلى Tauri Stronghold أو خادم وسيط (انظر دراسة التطوير).
- الفحص الكامل يرسل عدة طلبات بالتوازي؛ راقب حدود الاستهلاك (Rate Limit).

## 6) الاختبار قبل أي إصدار
- `npm run dev` ومعاينة كل تبويب.
- `npm run tauri dev` للتأكد من نافذة سطح المكتب.
- تأكّد أن التصدير (PDF/HTML) يعمل، وأن الوضعين الفاتح/الداكن سليمان.
